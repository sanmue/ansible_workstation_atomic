---

- name: Ansible galaxy collection
  hosts: localhost
  connection: local
  tasks:
    - name: Ansible galaxy collection - install community.general
      ansible.builtin.command: ansible-galaxy collection install community.general
      register: install_community_general
      changed_when: "'installed successfully' in install_community_general.stdout"
